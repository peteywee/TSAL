{
  "name": "@topshelf/tsal",
  "version": "0.2.1",
  "private": true,
  "description": "Top Shelf Automation Lifecycle standard and local-first conformance tooling",
  "type": "module",
  "bin": {
    "tsal": "./tooling/cli.mjs"
  },
  "scripts": {
    "doctor": "node tooling/cli.mjs doctor .",
    "test": "node --test",
    "version:check": "node tooling/version-policy.mjs sync",
    "verify": "node tooling/version-policy.mjs sync && node tooling/verify-repo.mjs && node tooling/cli.mjs doctor . && node tooling/cli.mjs doctor examples/r0-readonly && node --test"
  },
  "engines": {
    "node": ">=22"
  }
}
